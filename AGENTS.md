# 新規執筆の作業ルール

## この作業領域

`hirsrsk/GPT-Novel` の `main` は、新しいオリジナル小説の作業領域です。旧版の続編・改稿版として扱わず、新版の内容はユーザーとの新しい決定から作ります。

## 参照範囲

- 新版の物語上の根拠は、ユーザーが今回の新規執筆で提示した内容と、現在の `main` に新版用として記録した資料です。
- `legacy`、過去のコミット、旧本文、旧設定、旧プロット、旧引き継ぎメモ、旧執筆支援ルールを、新版執筆のために自動で取得・検索・参照しません。
- 過去の会話に旧作品の内容が含まれていても、新版の前提、展開候補、整合性の基準、文体見本として流用しません。
- `hirsrsk/GPT-Novel-Fanfic` の人物・設定・本文・展開を、新版オリジナルへ混ぜません。
- ユーザーが旧版からの継承・比較・確認を明示した場合だけ、指定範囲を参照します。指定していない人物関係・事件順・章構成などは一緒に継承しません。
- 保存確認のためのブランチ・コミット・ツリー・ハッシュの照合は、物語内容の参照とは区別します。

## 抽出済みの執筆作法

2026-09-10のユーザー依頼により、作品非依存の執筆作法だけを `writing_guides/` へ抽出しました。これは旧作品を継承する許可ではなく、現在の `main` にある抽出版を手順・点検に使うための限定的な追加です。

- 執筆・改稿には `writing_guides/執筆作法.md` と、依頼範囲に応じた `writing_guides/推敲チェック.md` を使います。
- 場面の事実を整理する必要がある場合だけ、`writing_guides/場面整理テンプレート.md` を使います。書式を全て埋めるために、未決定の設定・対立・事件を作りません。
- `writing_guides/文体・運用の選択肢.md` は候補集です。視点方式、文字数、場面数、作者像などを採用済みとして扱いません。決定は新作資料へ記録します。
- `writing_guides/抽出記録.md` は来歴確認用です。記載された元ファイル・コミット・ハッシュを、新版執筆のための取得先・必読資料にしません。
- 抽出版から原資料や旧スキルへ自動的に戻りません。書き方の一般則を人物の性格や筋書きへ変換しません。
- 表現の修正で構造や場面の確定事項を勝手に変えず、該当する層の問題として扱います。日常・余韻・生活の具体へ事件解決や伏線回収を強制しません。
- 執筆・修正が依頼されている場合、場面票の未作成や外部レビューの未実施だけを理由に、追加承認や別作業を要求しません。必要最小限を同じ依頼内で整理し、重要な未決定事項は未決定のまま明示します。

## 新版の決定

- 未決定の設定・人物・ジャンル・開始地点を、旧版や推測で埋めません。
- 提案と採用済みの決定を区別し、採用された内容だけを新版の確定事項として記録します。
- 旧版への接続、旧版との矛盾解消、旧版の話数や場面配置の再現を目標にしません。
- 本文・設定・構成は、必要になった時点で `main_novel/` 内に作成します。`writing_guides/` は作品固有の設定や場面の保存先にしません。

## 保存と変更

- `legacy` は保存用として維持し、明示的な指示なしに更新・削除したり、`main` にマージしたりしません。
- 別リポジトリへの変更は、そのリポジトリについてユーザーが指示した範囲でのみ行います。
- 文章はUTF-8のテキストとして保存し、保存後に再取得して、意図した内容と一致することを確認します。
